# Contributing

## Git Hooks

For development, please install the pre-commit hooks
```shell
> python3 -m venv .venv
> source .venv/bin/activate
> pip install -r requirements.txt
> ./configure-hooks.sh install
```
